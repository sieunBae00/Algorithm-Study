# [Gold III] 나머지 합 - 10986 

[문제 링크](https://www.acmicpc.net/problem/10986) 

### 성능 요약

메모리: 9836 KB, 시간: 120 ms

### 분류

수학, 누적 합

### 제출 일자

2026년 4월 4일 01:06:48

### 문제 설명

<p>수 N개 A<sub>1</sub>, A<sub>2</sub>, ..., A<sub>N</sub>이 주어진다. 이때, 연속된 부분 구간의 합이 M으로 나누어 떨어지는 구간의 개수를 구하는 프로그램을 작성하시오.</p>

<p>즉, A<sub>i</sub> + ... + A<sub>j</sub> (i ≤ j) 의 합이 M으로 나누어 떨어지는 (i, j) 쌍의 개수를 구해야 한다.</p>

### 입력 

 <p>첫째 줄에 N과 M이 주어진다. (1 ≤ N ≤ 10<sup>6</sup>, 2 ≤ M ≤ 10<sup>3</sup>)</p>

<p>둘째 줄에 N개의 수 A<sub>1</sub>, A<sub>2</sub>, ..., A<sub>N</sub>이 주어진다. (0 ≤ A<sub>i</sub> ≤ 10<sup>9</sup>)</p>

### 출력 

 <p>첫째 줄에 연속된 부분 구간의 합이 M으로 나누어 떨어지는 구간의 개수를 출력한다.</p>

---

### Idea

#### 처음 생각

`j-i` 의 크기도 정해지지 않았으므로, 모든 경우에 대해 한 칸씩 이동하며 더하고, 나머지를 계산한다면 분명 시간 초과가 날 것이다.  

-> '누적합' 이라는 알고리즘 분류 힌트를 봤다.  
  
미리 누적합을 계산해 놓자! -> 누적합 배열(`S[]`)  

> 💡 어떤 구간(`S[i]~S[j]`) 의 합 = __`S[j] - S[i-1]`__   
>  M으로 나누어떨어지는 구간합 구하기  
</br>

🛑 다 해보는 건 역시 시간초과가 뻔한데...  
어떤 구간합(`S[j] - S[i-1]`) 이 M으로 나누어 떨어지려면...??  
  
> 💡 __`S[j] - S[i-1]` 이 M으로 나누어 떨어진다__ = __`S[j]` 와 `S[i-1]` 을 각각 M으로 나누었을 때__, __나머지가 같다.__   
  
> 누적 합 배열을 구한 후, 다시 각 칸을 'M으로 나눈 나머지' 로 바꾼다. !!  
> 즉, 배열에 저장된 수는 `0~(M-1)` 사이의 정수가 된다. !!  
  
그 후... 배열을 순회하며 각 숫자의 개수를 센다. -> 따로 배열에 저장  
  
새로 만든 배열을 순회하며,  
> 2보다 크거나 같다면 -> "X개 중 2개를 택하는 경우의 수" 계산  
> -> __`X(X-1) / 2`__  

🛑 처음에 위 식으로만 계산해서 `ans` 에 더했다가, 원하던 값이 나오지 않아서 당황했다.  
  
-> 문제를 잘 읽자. 문제에서는 __`i = j` 인 경우도 허용__ 하고 있다.  
즉, 한 칸 자체로 M으로 나누어 떨어지는 경우(=나머지 0)도 인정하는 것이다. -> 따라서 "나머지가 0인 것들의 개수"를 추가로 더해주어야 한다. !!  
  
*자료형도 꼭 꼭 주의하자. 이 문제에서도 누적합 배열만 long long 으로 설정했다가 에러를 맞이했다.  
('나머지의 갯수' 도 int 자료형을 초과할 수 있으므로 해당 배열도 long long 으로 설정한다 !)  

